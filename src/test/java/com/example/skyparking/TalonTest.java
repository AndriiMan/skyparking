/*
package com.example.skyparking;

import com.example.skyparking.entity.Machine;
import com.example.skyparking.entity.Talon;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;
import org.mockito.Mock;

import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;

public class TalonTest {

    @Test
    public void getTalon() {
        Assertions.assertNotNull(talon);
    }

    @Test
    public void getTalonTimeIn() {
        DateTimeFormatter dateTimeFormatter = DateTimeFormatter.ofPattern("yyyy/MM/dd HH:mm");
        LocalDateTime timeForNow = LocalDateTime.now();
        Assertions.assertEquals(talon.getCurrentTime(), dateTimeFormatter.format(timeForNow));
    }

    @Test
    public void getTalonIsActive() {
        Assertions.assertTrue(talon.isActive());
    }



}
*/
